- provide :title, 'All users'
.center.hero-unit
  %h2 All users
  %ul.users
    - @users.each do |user|
      %li
        - if !current_user? user
          = link_to user.name, user   
  = will_paginate